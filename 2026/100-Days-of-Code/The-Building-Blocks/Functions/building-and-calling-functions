/* Giving our funciton declaraction a name
function listArrayItems(arr) {
    // more code to be added later...
}

function listArrayItems(arr) {
    for (var i = 0; i < arr.length; i++) {
         // ... code pending here.... Right now there is no code inside the loop's body.
         // ... This means the funciton currently does nothing with the array elements.
    }
   
}

// Decide is how I want to output each item from the recieved arr array.

function listArrayItems(arr) {
    for (var i = 0; i < arr.length; i++) {
        console.log(arr[i]); // display the array item where the index is equal to i
    }
}

// Invoke the "listArrayItems" function

var colors = ['red', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink'];
listArrayItems(colors); // display all items in the array at once.

// function that takes an array as input and display all items of this array
function listArrayItems(arr) {
    for (var i = 0; i < arr.length; i++) {
        console.log(i, arr[i])
    }
}
var colors = ['red', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink'];
listArrayItems(colors);

// count from one insted of zero

function listArrayItems(arr) {
    for (var i = 0; i < arr.length; i++) {
        console.log(i+1, arr[i])
    }
}
var colors = ['red', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink'];
listArrayItems(colors);
*/
// I can even add one or more conditions

function listArrayItems(arr) {
    for (var i=0; i < arr.length; i++)
        if (arr[i] == 'red') {
            console.log(i*100, "tomato!")
        } else {
            console.log(i*100, arr[i])
        }
}
var colors = ['red', 'orange', 'yellow', 'green', 'blue', 'purple', 'pink'];
listArrayItems(colors);
