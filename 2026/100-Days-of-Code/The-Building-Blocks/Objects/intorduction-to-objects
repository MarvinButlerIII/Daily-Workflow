// improvements you could make in your code; long list of varibale names and getting JavaScript to understand they're related

var storeManager = {}
storeManager .rangeTilesPerTurn = 4;
storeManager .socialSkills = 50;
storeManager .streetSmarts = 50;
storeManager .health = 30;
storeManager .specialAbility = 'finding business opportunities';
storeManager .greeting = "let's make some money";

// inspect the store manager object it recognizes these a traits of the store manager
Movement = 4;
socialSkills = 50;
streetSmarts = 50;
Health = 30;

// new character object and assign objects to its properties

var assistantManager = {
    movement: 3,
    socialSkills: 30,
    streetSmarts: 30,
    health: 40
}

// Dot notation can alo assingn new character object and assign objects to its properties

storeManager .nextAchievement = 'open a new store';
assistantManager .nextAchievement = 'get promoted';